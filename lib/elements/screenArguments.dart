import 'dart:ui';

class ScreenArguments {
  final dynamic data;
  final Color backgroundColor;
  final Color color;
  final String type;

  ScreenArguments(this.data, {this.backgroundColor, this.color, this.type});
}